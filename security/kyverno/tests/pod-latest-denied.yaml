apiVersion: v1
kind: Pod
metadata:
  name: policy-negative-latest
  namespace: edl-data
spec:
  restartPolicy: Never
  containers:
    - name: nginx
      image: nginx:latest
      resources:
        requests:
          cpu: 10m
          memory: 16Mi
        limits:
          cpu: 50m
          memory: 64Mi
