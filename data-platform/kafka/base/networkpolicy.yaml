apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-kafka-same-namespace
  namespace: edl-data
spec:
  podSelector:
    matchLabels:
      strimzi.io/cluster: edl-kafka
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
